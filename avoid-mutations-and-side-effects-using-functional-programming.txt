// The global variable
let fixedValue = 4;

function incrementer() {
  // Only change code below this line
let copyValue = fixedValue + 1;
return copyValue
  // Only change code above this line
}

console.log(incrementer())